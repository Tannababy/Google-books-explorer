function Card(props) {
  return (
    <section>
      {/* <image src={props.image} alt={props.title} />
      <p>{props.title}</p>
      <p>{props.author}</p>
      <p>{props.description}</p> */}
    </section>
  );
}
export default Card;
